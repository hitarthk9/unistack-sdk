langgraph>=1.2,<2.0
langgraph-checkpoint-mongodb>=0.4
pymongo>=4.16.0
anthropic>=0.40.0
opentelemetry-sdk>=1.30,<2
opentelemetry-exporter-otlp-proto-http>=1.30,<2   # OTLP/HTTP protobuf — what Langfuse ingests

# Optional (graph-runtime server / `unistack serve`):
#   pip install -e ".[server]"   → fastapi, uvicorn
